@model List<Aplicacao.ViewModels.PagamentoReembolsoViewModel>

@{
    Layout = null;
}

<div id="modalInformacoes" class="modal fade" role="dialog" style="width:auto; ">
    <div class="modal-dialog" style="width:700px">
        <div class="modal-content col-xs-12" style="width:700px;  background-color: lightgray">
            <div id="modalBodyInformacoes" class="modal-body col-xs-12" style=" background-color: lightgray; height: 500px;overflow-y: auto;">
                <p class="text-center">Recibos Informados</p>
                <br />

                @if (Model != null)
                {

                    foreach (var conta in Model)
                    {
                        <div class="row">
                            <div style="border: solid 1px;">


                                <label>Numero de Cobrança: @(conta.ContasAPagarViewModel.CodigoAgrupadorParcela) </label>
                                                             

                                <p><label>Numero do Recibo: @(conta.NumeroRecibo)</label></p>


                                <p><label>Valor Pago: @(conta.ContasAPagarViewModel.Valor) </label></p>


                                <p><label>Data do Pagamento: @(conta.DataInsercao)</label></p>

                            </div>
                        </div>
                        <br />

                        @*<div class="row">
                                <div class="col-xs-3 col-sm-3">
                                    <label>@(Model.Id.ToString().PadLeft(6, '0'))</label>
                                </div>
                                <div class="col-xs-3 col-sm-3">
                                    <label>@(Model.ContasAPagar.TipoPagamento)</label>
                                </div>
                                <div class="col-xs-3 col-sm-3">
                                    <label>@(string.Format("R$ {0:C}", Model.ContasAPagar.Valor))</label>
                                </div>
                                <div class="col-xs-3 col-sm-3">
                                    <label>@(Model.ContasAPagar.DataVencimento.ToShortDateString())</label>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-2 heightline">
                                    <label>Observações:</label>
                                </div>
                                <div class="col-sm-10">
                                    <label>@(Model.Observacoes)</label>
                                </div>
                            </div>*@
                    }
                }

            </div>
            <div class="modal-footer">
                @*<div class="col-xs-12 col-sm-12">
                        <div class="pull-right margin-right-10">
                            <button class="btn btn-lg btn-danger" data-dismiss="modal" aria-label="Close">
                                Cancelar
                            </button>
                        </div>
                        <div class="pull-right margin-right-10">
                            <button type="submit" class="btn btn-lg btn-primary" onclick="Informar();">
                                Informar
                            </button>
                        </div>
                    </div>*@
            </div>
        </div>
    </div>
</div>