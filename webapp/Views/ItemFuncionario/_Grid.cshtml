@using Core.Extensions;

@model List<ItemFuncionarioViewModel>

<div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div class="jarviswidget jarviswidget-color-blueDark" id="wid-id-1" data-widget-editbutton="false" data-widget-deletebutton="false">
            <header>
                <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                <h2>Lista de Registros</h2>
            </header>

            <div>
                <div class="widget-body no-padding">
                    <table id="datatable_fixed_column" class="table table-striped table-bordered" width="100%" style="margin-top: 0px !important">
                        <thead>
                            <tr>
                                <th class="hasinput" style="width:10%">
                                    <input type="text" class="form-control" placeholder="Filtrar Funcionário" />
                                </th>
                                <th class="hasinput" style="width:10%">
                                    <input type="text" class="form-control" placeholder="Filtrar Data Entrega" />
                                </th>
                                <th class="hasinput" style="width:10%">
                                    <input type="text" class="form-control" placeholder="Filtrar Data Devolução" />
                                </th>
                                <th class="hasinput" style="max-width: 120px; min-width: 120px; width: 1%;" rowspan="1" colspan="1">
                                </th>
                            </tr>
                            <tr>
                                <th data-hide="phone">Funcionário</th>
                                <th data-hide="phone">Data Entrega</th>
                                <th data-hide="phone">Data Devolução</th>
                                <th data-class="expand">Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Funcionario.Pessoa.Nome</td>
                                    <td>@(item.DataEntrega?.ToShortDateString())</td>
                                    <td>@(item.DataDevolucao?.ToShortDateString())</td>
                                    <td>
                                        <a class="btn btn-primary" href="@Url.Action("edit", new {Id = item.ItemFuncionarioId})"><i class='fa fa-edit'></i></a>
                                        <a class="btn btn-danger" href="@Url.Action("delete", new {Id = item.ItemFuncionarioId})"><i class="fa fa-remove"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </article>
</div>