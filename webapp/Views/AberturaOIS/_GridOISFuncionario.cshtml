@using Entidade.Uteis
@model List<OISFuncionarioViewModel>

<div class="row">
    <div class="col-xs-12">
        <table id="tablevigencia" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th style="width: 60px;">Ações</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Funcionario.Pessoa.Nome
                            </td>
                            <td style="width: 60px">
                                <button type="button" name="botaoedicao" class="btn btn-danger" style="margin-right: 3px;" onclick="removerOisFuncionario(@item.Funcionario.Id)"><i class="fa fa-remove"></i></button>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>