@model List<ContasAPagarViewModel>

@{
    Layout = null;
}

<div id="modalInformaRecibo" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content col-xs-12">
            <div id="modalBodyInformarRecibo" class="modal-body col-xs-12" style="max-height:320px; overflow: auto;">
                <p class="text-center">Deseja informar Recibos?<br />Se sim, informe:</p>
                <br />
                @{
                    if (Model != null && Model.Count() > 0)
                    {
                        for (int i = 0; i < Model.Count(); i++)
                        {
                            var item = Model.ToList()[i];

                            <div class="col-xs-12 col-sm-12">
                                <div class="col-xs-4 heightline">
                                    <label>Numero de Cobrança:</label>
                                </div>
                                <div class="col-xs-8 heightline">
                                    <label>@(item.Id.ToString().PadLeft(6, '0'))</label>
                                </div>
                                <div class="col-xs-4 heightline">
                                    <label>Numero de Recibo:</label>
                                </div>
                                <div class="col-xs-8 heightline">
                                    <input id="numero-recibo-@(item.Id)" data-numero-recibo data-conta-id="@item.Id" class="heightfield" style="width:100%;" onblur="somenteNumeros(this)" onkeypress="validaNumeros(event);" maxlength="40" />
                                </div>
                                <div class="col-xs-4 heightline">
                                    <label>Valor pago:</label>
                                </div>
                                <div class="col-xs-8 heightline">
                                    @*<label>@(string.Format("R$ {0:C}", item.Valor))</label>*@
                                    <label>@(Convert.ToDecimal(item.Valor).ToString("C2"))</label>
                                </div>
                                <div class="col-xs-4 heightline">
                                    <label>Data do Pagamento:</label>
                                </div>
                                <div class="col-xs-8 heightline" style="margin-bottom:30px;">
                                    <label>@(item.DataVencimento.ToShortDateString())</label>
                                </div>
                            </div>
                        }
                    }
                }
            </div>
            <div class="modal-footer">
                <div class="col-xs-12 col-sm-12">
                    <div class="pull-right margin-right-10">
                        <button class="btn btn-lg btn-danger" data-dismiss="modal" aria-label="Close">
                            Cancelar
                        </button>
                    </div>
                    <div class="pull-right margin-right-10">
                        <button type="submit" class="btn btn-lg btn-primary" onclick="Solicitar();">
                            Solicitar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>